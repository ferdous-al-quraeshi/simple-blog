<p>
  <strong>Title:</strong>
  <%= @article.title %>
</p>
 
<p>
  <strong>Text:</strong>
  <%= @article.text %>
</p>

<h2>Comments</h2>
<%= render @article.comments %> <%# the `render` method iterates over the `@article.comments` collection %>

<h2>Add a comment:</h2>
<%= render 'comments/form' %>

<p>
  <%= link_to 'Edit', edit_article_path(@article), :class=>"btn btn__edit" %>
  <%= link_to 'Back', articles_path, :class=>"btn btn__back" %>
</p>